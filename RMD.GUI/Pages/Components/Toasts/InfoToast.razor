@if (ShowInfoToast)
{
	<div class="toast_container">
		<div class="toast_contents">

			<i class="bi bi-info-circle-fill"></i>

			<div class="text_container">
				<p class="text-truncate" style="max-width:350px; height: 100%;">@InfoToastMessage</p>
			</div>
			<i class="bi bi-x" @onclick="Close"></i>

		</div>
	</div>
}


@code {
	[Parameter] public bool ShowInfoToast { get; set; }
	[Parameter] public string InfoToastMessage { get; set; } = string.Empty;
	[Parameter] public EventCallback OnClose { get; set; }

	private async Task Close()
	{
		if (OnClose.HasDelegate)
		{
			await OnClose.InvokeAsync();
		}
	}

}

